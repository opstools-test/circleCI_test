version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: echo "hello world"
orbs:
  jira: circleci/jira@1.0.5

workflows:
  build:
    jobs:
      - build:
          post-steps:
            - jira/notify
